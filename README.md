# db_listener. 
Двухфазный обработчик событий: потребляет уведомления из PostgreSQL, выполняет бизнес-логику через модификацию данных в БД и генерирует коррелированные события в Kafka топик, реализуя паттерн Transactional Outbox.
